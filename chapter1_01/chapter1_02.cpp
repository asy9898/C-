
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main() {

	int a, b, c;

	printf("첫번째 값을 입력하세요.");
	scanf_s("%d",&a);
	printf("두번째 값을 입력하세요.");
	scanf_s("%d", &b);
	
	//c = a + b;
	//printf("덧셈결과 = %d\n", c);
	//c = a%b;
	//printf("나머지결과 = %d\n", c);
	//c = -a+b;
	//printf("음수덧셈결과 = %d\n", c);
	//c = a+1;
	//printf("a의 증가값 = %d", c);

	// _~to~_s = 타입 변환을 위한 명령문 _atoi_s(바꾸려는 int 변수명) = 문자열을 int형으로 _itoa_s(바꾸려는 문자 변수명) = int형을 문자형으로







	return 0;
}

